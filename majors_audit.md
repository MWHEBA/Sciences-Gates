# جرد حقول الداشبورد الناقصة للتخصصات (Majors Audit)

هذا الملف يحتوي على جرد لجميع التخصصات الموجودة في قاعدة البيانات والتي تحتوي على حقول ناقصة (فارغة تماماً ولا تحتوي على أي حرف أو قيمة) في لوحة التحكم (الداشبورد)، وذلك بعد استبعاد التخصصات المكتملة تماماً، واستبعاد الحقول التالية من الحسابات بناءً على طلبك:
* المقالات المرتبطة (`related_articles`)
* وصف Open Graph (`og_description`)
* النص البديل للصورة الرئيسية (`main_image_alt`)
* الرابط الأساسي (`canonical_url`)
* رابط التخصص عند المنافس (`competitor_url`)
* أفضل الجامعات (`best_universities`) - تم ملؤها بالكامل
* الجامعات الاقتصادية (`cheap_universities`) - تم ملؤها بالكامل

---
